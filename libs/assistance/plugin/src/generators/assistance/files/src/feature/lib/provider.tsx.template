import 'server-only';

import { createAI } from 'ai/rsc';
import { AIState, UIState } from '@wrkspce/assistance/domain';
import FeatureActions from './actions/index.tsx';
import { ToolActions } from './tools/index.tsx';
const initialAIState: AIState[] = [];
const initialUIState: UIState[] = [];

export const AssistanceProjectProvider = createAI({
  actions: {
    ...FeatureActions, ...ToolActions
  },
  initialUIState,
  initialAIState,
});
